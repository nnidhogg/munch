# Window-planned scaling, development machine, 2026-08-08

Full output (summary.txt) and every timed scaling observation (observations.csv) of the run the repository
README's window-planned scaling section quotes: commit 802fdf0, clean tree, 15 interleaved rounds at 1, 16,
128, and 512 MiB under WSL2 on the development machine, collected 2026-08-08 21:40 UTC. This is the same
machine and framing as the repository's historical development numbers and a different setup from the
bare-metal archives beside it; ratios compare within one collection, never across collections. An identical
run minutes earlier from the same sources before they were committed agreed with this one within one percent
on every 512 MiB median and is not archived, since a figure is quotable only beside a clean commit.
